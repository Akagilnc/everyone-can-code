# 定义一个打印指定字符串str中，最长的按照字母表顺序顺排的子字符串str_temp的函数print_longest_substr

# 对字符串str进行切片处理，切片规则如下

''' 当一个字母letter2在字母表中的位置在前一个字母letter1之后时，以letter1为起点向后查找，
    当发现letter2在letter1之前时，打印结果并停止向后查找'''

# 获取输入的小写英文字母字符串

# 调用函数
